# we must pass 2 variables
# 1. component
# 2. environment
- name: "configure {{ component }} server"
  hosts: all
  become:  yes
  vars_files:
  - "variable-{{ environment }}.yaml" # this is ansible server

  roles:
  - "{{ component }}"